name: Clean Caches

on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

permissions: {}

jobs:
  clean-caches:
    name: Clean Caches
    permissions:
      contents: read
    uses: JackPlowman/reusable-workflows/.github/workflows/common-clean-caches.yml@054a910869922f7c75c30d26cca299b8cf242584 # v2025.06.12.01
    secrets:
      workflow_github_token: ${{ secrets.GH_TOKEN }}
